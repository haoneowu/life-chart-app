---
description: Update project management files (speckit) based on current progress
---
1. Inspect the codebase to verify the implementation status of tasks listed in `speckit.plan` and `speckit.tasks`.
2. Update `speckit.tasks` checking off completed items.
3. Update `speckit.plan` reflecting phase completion.
4. Report the summary of updates to the user.
